import styled from 'styled-components';

export const Wrapper = styled.label`
    color: inherit;
    cursor: pointer;
    display: inline-flex;
    font-family: inherit;
    font-size: 100px;
    line-height: 1.5;

    .RequiredIndicator {
        font-weight: bold;
        margin-left: 2px;
    }

    .TooltipWrapper {
        margin-left: 5px;
    }
`;
